export enum eventPriorites {
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low',
}

export enum STATUS {
  IDLE = 'idle',
  LOADING = 'loading',
  SUCCESS = 'success',
  ERROR = 'error',
}

export enum eventCategories {
  ART = 'Art',
  MUSIC = 'Music',
  BUSINESS = 'Business',
  CONFERENCE = 'Conference',
  WORKSHOP = 'Workshop',
  PARTY = 'Party',
  SPORT = 'Sport',
}
